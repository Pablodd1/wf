# Required external artifacts

Place these files here before execution:

- `rc50-code-handoff.tar.gz`
- `rc50-evidence-handoff.tar.gz`

Verify them against `../EXPECTED_SHA256SUMS.txt`. These archives can contain local evidence and must not be committed or pushed. The `.gitignore` in this directory keeps archive and extraction contents local.
