# WatchFacts POC — Project Handoff (2026-08-02)

## Current Production State (commit 23d1050, main)
All changes pushed and live at watchfacts-poc.vercel.app.

### Working Endpoints
| Endpoint | Status | Time | Notes |
|---|---|---|---|
| /api/price-research | 200 | ~1s | Public, full analytics, raw_message, outliers, liquidity |
| /api/reviewed-market-inventory | 200 | ~1.4s | 2.5M records, client-side images-first |
| /api/model-stats | 200 | ~1.8s | Fixed (was crashing since a9598e4) |
| /api/catalog-models | 200 | <1s | Catalog-index-first strategy |

### What Was Fixed Today
1. **Price Research auth regression** — removed authorizeDealer + DealerGate (commit 57cdd51)
2. **"Reviewed price:" label** — stripped from TradingFloor cards
3. **Evidence filter toggle** — removed from desktop + mobile
4. **Trading Floor 503** — images-first server-side sort timed out on unindexed view; reverted server sort, added client-side stable partition
5. **model-stats/dealer-stats crash** — missing api/_lib/cors.js since a9598e4; created + wired catalog loader
6. **catalog-models timeout** — large brands (RM, Cartier) hit Supabase view scan; switched to in-memory catalog-first

### Test Suite
632/632 pass, tsc clean.

---

## ACTIVE USER REQUEST: Show Raw Listing Per Watch

### What's Done
- **Price Research drill-down**: Already renders `raw_message` in an "Original listing" panel in the detail modal (src/pages/PriceResearch.tsx ~line 1895+)
- **API layer**: `price-research-listing.js` returns `raw_message` and `raw_message_scope`

### What's NOT Done — Next Steps
1. **Trading Floor cards need raw_message**
   - Check if `/api/reviewed-market-inventory` already returns `raw_message` per record (likely YES — the view `reviewed_workbook_market_source_v2` probably has it)
   - If yes: add a collapsible "Original listing" section to each TradingFloor card (similar to PriceResearch modal)
   - If no: add `raw_message` to the SELECT in `api/reviewed-market-inventory.js` and pipe it through

2. **TradingFloor card raw_message UI**
   - File: `src/pages/TradingFloor.tsx`
   - Add a `<details>` or collapsible panel under each card showing the raw source text
   - Match the PriceResearch "Original listing" panel styling
   - Consider mobile layout (collapsed by default on mobile, expanded on desktop click-through)

3. **Flash sale detail pages** — same raw_message display needed for `/flash-sales/:id` detail view

---

## Known Issues / TODOs (from user's original list)

### Remaining from original task list
- [ ] price-research-eligibility.cjs — count MISSING_PRICE/currency rejections, don't drop
- [ ] Multi-key Trading Floor/Price Research sort: evidence→price→image→newest
- [ ] Image pipeline: source-identity match + reachability check, remove frame if invalid
- [ ] Per-brand completeness counts (has_price/has_image/has_seller/analytics_eligible)

### Technical Debt Found
- `dealer-stats.js` also imports `_lib/cors` — verify it works live (same fix as model-stats)
- PostgREST view `reviewed_workbook_market_source_v2` has no index on `has_exact_source_image` — adding a partial index would enable server-side images-first sort
- MiniMax M3 API via NVIDIA NIM endpoint continues to timeout (unrelated to WF)

---

## Key Files Reference
- `api/reviewed-market-inventory.js` — Trading Floor API (sort, pagination, enrichment)
- `src/pages/TradingFloor.tsx` — Trading Floor frontend (1072 lines, card rendering ~line 900+)
- `src/pages/PriceResearch.tsx` — Price Research frontend (raw_message panel ~line 1895+)
- `api/price-research.js` — Price Research API (public, analytics, outliers)
- `api/price-research-listing.js` — Per-listing detail (raw_message, outlier_rows, liquidity)
- `api/model-stats.js` — Per-model stats (fixed: catalog loader + cors.js)
- `api/_lib/cors.js` — NEW: shared CORS helper (was missing, caused model-stats crash)
- `api/_lib/catalog.js` — Shared catalog loader (listCatalogReferences)
- `api/_lib/reviewed-workbook-analytics.cjs` — Analytics loader (row filter removed)
- `api/catalog-models.js` — Catalog-model browse (catalog-index-first strategy)

## DB Connection Details
- Supabase: `db.bptrvfncppbjnchsaxtb.supabase.co:5432` user=postgres pw=197jGFJ7YIJwtYj2
- View: `reviewed_workbook_market_source_v2` (2.5M rows, unindexed on has_exact_source_image)
- MySQL @ 161.35.0.209:3306 (OceanDigital, READ-ONLY)
