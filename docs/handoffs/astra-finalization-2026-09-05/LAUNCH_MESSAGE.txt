Act as the sole integration and release lead for WatchFacts / Curated Luxury. Open https://github.com/Pablodd1/wf on branch codex/astra-finalization-package, verify the remote commit against the owner's transfer SHA, and read docs/handoffs/astra-finalization-2026-09-05/START_HERE.md, CURRENT_EXECUTION_STATE.md, PRODUCT_ACCEPTANCE.md and MASTER_EXECUTION_PROMPT.md completely. Read AGENTS.md and its mandatory handoffs from the application checkout before acting on that code.

Execute the full assignment: preserve and integrate the deployed history, close the remaining provenance/security/pagination/scalability blockers, prove disposable PostgreSQL 15/18 plus genuine Supabase/PostgREST and Vercel, publish the first 50 real eligible listings, then continue through the entire verified frozen source boundary, normalization, durable Supabase population, all eligible Trading Floor and Price Research records, dealers/contact, bundles after singles, and the final live website. RC50 is a checkpoint, not completion.

This is a documentation-and-patch package, not a deployable application branch. It includes the exact RC50 patch and tested follow-up file replacements. Do not reapply RC50 to the existing local codex/rc50-final-integration worktree or merge unrelated histories. Preserve that worktree and the owner's unrelated edits; verify the actual deployed SHA and forward-port onto that history. Apply the supplied safe replacements for the legacy credential-bearing static HTML pages before builds; owner credential revocation/rotation is still required. No production mutation before the mandatory disposable gates and rollback are proven. Never treat local simulated JWT/RLS/RPC results as genuine Supabase/Vercel evidence.

Self-review and repair root causes, keep an exact evidence and mutation ledger, never invent missing facts or weaken tests, and continue without repeated routine approvals. Pause only for an essential missing artifact/credential, a bounded external-access failure, an unsafe live-state discrepancy, new paid-resource authorization, or irreversible destruction outside scope. Finish with the executed live outcome and exact reconciled counts, or the precise blocker and last safe checkpoint. Do not stop after 50 or produce another plan instead of executing.
