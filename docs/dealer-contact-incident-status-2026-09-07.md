# Dealer-contact incident status and owner acknowledgment

Recorded September 7, 2026, 20:59 UTC.

The owner has read and acknowledged the dealer-contact privacy incident. Do not request acknowledgment of that same report again. Acknowledgment is not resolution. Initial status at acknowledgment: **OPEN_PENDING_DEPLOYED_PUBLIC_RESPONSE_VERIFICATION**. Current status after the checks below: **RESOLVED_VERIFIED_CORRECTIVE_DEPLOYMENT** (21:03 UTC).

Preserve approved contact through opaque server-side actions. Phone numbers must not appear in public API payloads, HTML or the DOM. Do not expand consent to additional dealers. Ask only for a specific new decision, missing access or additional authorization that is actually necessary.

At 20:58:41 UTC the additional poster-identity importer (owned PID 29408) was stopped under that restriction. The last local committed checkpoint was 3,075 identities; independently reconcile the database for any final in-flight transaction. A local pause guard prevents automatic restart. Existing consented contacts remain authorized. Listing publication and frozen-boundary processing continue. The earlier broader import plan does not authorize further consent additions under the latest instruction.

The last independently verified live application is `ff7ec7dd4f70ae669b1d8e08ca8053952e9eba8c`, deployment `dpl_39M2N2sohpc9bmFZynG5cwAZEQov`, at https://wf-ecru.vercel.app. Fresh exact-version, public-response and rendered-DOM privacy verification is required before closing this incident. Earlier successful receipts alone do not close this status entry. The prior application rollback target is `dpl_uQi4CCDwTuiU2MsWLQPve2iftd5M`.

Normalization completed at 20:39:04 UTC: all 1,527,898 inputs have durable normalization outcomes: 104,160 preliminary single candidates, 923,888 review, 499,842 bundle-held, eight quarantine, zero errors. Preliminary candidates remain subject to materialization and publication gates. At 20:58 UTC, full materialization had processed 792,000 inputs and the progressive publisher recorded 61,715 committed listings. These are distinct stages. Full completion is not claimed. There are 66 reviewed forward migrations.

Continue singles, immutable raw evidence and original listing image URLs. Bundles remain deferred. No storage deletion, generated listing images or additional paid resources are authorized by this incident acknowledgment.

## Corrective deployment verification

Fresh live version checks at 21:00–21:03 UTC confirmed `ff7ec7dd4f70ae669b1d8e08ca8053952e9eba8c`. Public profile and contact JSON tests passed for an existing verified dealer and a newly imported, explicitly unverified source poster. Private comparisons verified the exact original phone destination and selected listing inquiry through the opaque server action. Unknown profiles and invalid channels were denied. No external conversation was opened and no message was sent.

Initial HTML and rendered desktop/mobile DOM checks passed for both profile types: no tested source phone numbers or direct phone links were exposed, contact actions remained opaque, and no API failures or horizontal overflow occurred. The source poster retained the truthful verification-unavailable label and null unsupported ratings/reviews. The first browser attempt failed a case-sensitive label assertion because CSS renders uppercase; the corrected case-insensitive text check passed. That failed attempt is preserved as test evidence, not hidden or characterized as a product correction.

Local receipts: `outputs/live-source-poster-api.json`, `outputs/live-imported-poster-api.json`, and `outputs/live-contact-incident-dom.json`. This resolves the identified profile/contact disclosure regression on the verified deployed commit. It is not a claim that every historical row or all product acceptance checks have completed. Full publication and final snapshot audits remain active.

Read-only reconciliation confirmed exactly 3,075 committed source-poster identities at the import pause, with no retained import session or in-flight transaction. The remaining 128 planned identities were not imported. Existing-card reconciliation may link only already consented identities; it does not create dealers or change consent. Its backup and mutation receipts are preserved separately.
